services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: polla_db
      POSTGRES_USER: polla_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - dokploy-network
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U polla_user -d polla_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}
    networks:
      - dokploy-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build: ./backend
    # DATABASE_URL / REDIS_URL must use the same passwords as postgres/redis above (root .env).
    environment:
      DATABASE_URL: postgresql+asyncpg://polla_user:${POSTGRES_PASSWORD}@postgres:5432/polla_db
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      APP_ENV: production
      DEBUG: "false"
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 15
      REFRESH_TOKEN_EXPIRE_DAYS: 7
      CORS_ORIGINS: https://redcardtech.uk
      LOG_LEVEL: INFO
      SENTRY_DSN: ${SENTRY_DSN:-}
    networks:
      - dokploy-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads
    restart: unless-stopped

  # Build arg NEXT_PUBLIC_API_URL is baked into the bundle (CSP + API base).
  # Prod Dokploy: set https://redcardtech.uk or leave empty (same origin via nginx).
  # WebSocket uses wss://same-host/api/v1/ws/notifications — no separate WS env var.
  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-}
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-}
    networks:
      - dokploy-network
    depends_on:
      - backend
  # Entrada unificada (API + web): mismo aviso Windows — prueba http://127.0.0.1 si localhost falla.
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    labels:
      - traefik.enable=true
      - traefik.docker.network=dokploy-network
      # Solo HTTP → HTTPS (entrypoint web)
      - traefik.http.routers.polla-http.rule=Host(`redcardtech.uk`)
      - traefik.http.services.polla-web.loadbalancer.server.port=80
    networks:
      - dokploy-network
    depends_on:
      - backend
      - frontend

  # Backup diario a medianoche (TZ): PostgreSQL + fotos de perfil → volumen backup_data
  db-backup:
    build: ./scripts/backup
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: polla_db
      POSTGRES_USER: polla_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: ${TZ:-America/Lima}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-30}
    volumes:
      - backup_data:/backups
      - uploads_data:/uploads:ro
    networks:
      - dokploy-network
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  uploads_data:
  backup_data:

networks:
  dokploy-network:
    external: true
 